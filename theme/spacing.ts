export const spacing = {
    // Proportional 8pt grid scale
    none: 0,
    xxs: 4,      /* Micro gaps */
    xs: 8,       /* Inner card padding (tight) */
    sm: 12,      /* Regular padding (between lines) */
    md: 16,      /* Margins between elements */
    lg: 24,      /* Standard container padding / spacing between big blocks */
    xl: 32,      /* Section spacing */
    '2xl': 48,   /* Large whitespace gaps */
    '3xl': 64,   /* Major section dividers */
    '4xl': 96,   /* Hero spacing or very large whitespace */
};

export const layout = {
    borderRadius: {
        none: 0,
        sm: 8,       // Chips, tags, small inputs
        md: 12,      // Standard input fields, default buttons
        lg: 16,      // Default cards, large buttons (gen-z standard 14-16px)
        xl: 20,      // Modals, expanded card components (soft organic)
        '2xl': 24,   // Bottom sheets, large UI boundaries
        full: 9999,  // Avatars, action items, round chips
    },
};
